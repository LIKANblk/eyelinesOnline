get.clf.res <- function(path, fixation_threshold, epoch_size, left_border, sRate, bigFatMatrix)
{
  l <- eye_loaddata(path, fixation_threshold, epoch_size, left_border, sRate, bigFatMatrix)
  
  
  #get A1ch, A2ch
  
  
}